<div class="space-y-8">
  <section class="bg-white p-6 rounded shadow">
    <h1 class="text-2xl font-bold">草野球チーム（大阪）</h1>
    <p class="mt-2">毎週日曜午前に市内で活動。中級・社会人中心、楽しく＆本気。</p>
  </section>

  <section class="bg-white p-6 rounded shadow">
    <h2 class="text-xl font-semibold mb-3">次の試合</h2>
    <ul class="list-disc pl-6">
      <% Game.where("date >= ?", Date.today).order(:date).limit(3).each do |g| %>
        <li><%= g.date %>（<%= g.start_time.presence || "時間未定" %>）@ <%= g.place %> ／ vs <%= g.opponent %></li>
      <% end %>
    </ul>
    <div class="mt-3"><%= link_to "試合一覧へ", games_path, class: "underline" %></div>
  </section>

  <section class="grid md:grid-cols-2 gap-6">
    <div class="bg-white p-6 rounded shadow">
      <h2 class="text-xl font-semibold mb-3">最新お知らせ</h2>
      <ul class="list-disc pl-6">
        <% News.order(published_on: :desc).limit(2).each do |n| %>
          <li><%= n.published_on %>：<%= link_to n.title, n %></li>
        <% end %>
      </ul>
      <div class="mt-3"><%= link_to "お知らせ一覧へ", news_index_path, class: "underline" %></div>
    </div>

    <div class="bg-white p-6 rounded shadow">
      <h2 class="text-xl font-semibold mb-3">チームの特徴</h2>
      <ul class="list-disc pl-6">
        <li>活動地域：大阪市内（鶴見〜城東）</li>
        <li>レベル感：中級（大会C〜B）</li>
        <li>方針：全員出場・楽しく＆本気</li>
      </ul>
      <div class="mt-3"><%= link_to "チーム紹介を見る", team_path, class: "underline" %></div>
    </div>
  </section>

  <section class="text-center">
    <%= link_to "対戦／体験の申し込みはこちら", contact_path, class: "inline-block border px-4 py-2 rounded hover:bg-gray-100" %>
  </section>
</div>
